import {makeStyles} from '@material-ui/core/styles'
const useStyles = makeStyles((theme) => ({
    icon:{
        marginTop:150,
        width:166,
        height:166,
    },
    container:{
        display: 'flex',
        justifyContent: 'center'
    },
    text:{
        textAlign: 'center'
    }
}))
